name: build

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: "0 8 * * 1"

permissions:
  contents: write

jobs:
  Warp_Chrome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4.7.1
        with:
          python-version: '3.10'
      - name: prepare env
        run: |
          python3 -m pip install -r requirements.txt
          python3 run.py
      - name: Delete old asset
        run: gh release delete-asset nightly Chrome_Portable.7z -y || true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly
          name: Chrome Portable 便携版
          prerelease: true
          files: build/release/Chrome_Portable.7z
          generate_release_notes: false
          body: |
            ## Google Chrome 便携版

            解压后为 Chrome 文件夹，为 Chrome.exe 创建快捷方式即可使用。

            - 数据目录：Chrome\Data
            - 缓存目录：Chrome\Cache
            - 配置文件：Chrome\chrome++.ini

            > 每周一自动构建，始终覆盖为最新版。
